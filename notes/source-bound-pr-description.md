# Source-bound mental models and attributed Evaluation

This branch is stacked on `feat/coordinate-complete-new-yawn-v0-1` / PR #25.

It adds a source-preserving, correctable mental-model package and makes Evaluation the attributed protocol record beneath the human-facing label Judgment.

The durable substrate remains a typed relationship graph. The familiar line—Question → Position → Expression provision → Projection → Move → Consequence → Evaluation → Proof → Update → Question′—is explicitly a noncanonical traversal View.

The sanitized `dave/kickstarter/nestheads` fixture contains a governing Question, proposed Positions, an expression provision, a draft founder-message Projection, a proposed review/reconstruct/send Move, and one draft-readiness Evaluation. It contains no exact private source text, send claim, backer Consequence, Proof, canonical Update, publication authorization, or active runtime Agent.

Review must reconcile overlap across PRs #14, #23, #24, and #25 before merge. No deployment, publication, message send, private-source disclosure, runtime activation, or other external effect is authorized.
